# ============================================
# THE AVENGERS — .dockerignore
# Keep the Docker build context clean and secure
# ============================================

# Git
.git
.gitignore

# Secrets — NEVER include in Docker image
.env
.env.*
*.key
*.pem
secrets/
credentials/

# Documentation (not needed at runtime)
README.md
PLAN.md
CLAUDE.md
LICENSE
CHANGELOG.md
CONTRIBUTING.md

# Development
node_modules/
.vscode/
.idea/
.claude/
.tmpdata/
vignesh07-*

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Runtime data (created at runtime, not build time)
data/

# Supabase schema (not needed at runtime — run manually)
supabase/

# Docker files (prevent recursive context)
docker-compose*.yml
.dockerignore

# Old files (no longer used)
start.sh
railway.json
openclaw.json
